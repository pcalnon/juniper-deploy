{{- if and .Values.cascor.enabled .Values.cascor.persistence.snapshots.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "juniper.cascor.fullname" . }}-snapshots
  labels:
    {{- include "juniper.labels" . | nindent 4 }}
    app.kubernetes.io/component: cascor
spec:
  accessModes:
    - {{ .Values.cascor.persistence.snapshots.accessMode }}
  {{- $storageClass := .Values.cascor.persistence.snapshots.storageClass | default .Values.global.storageClass }}
  {{- if $storageClass }}
  storageClassName: {{ $storageClass | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.cascor.persistence.snapshots.size }}
{{- end }}
