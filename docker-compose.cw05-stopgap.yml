# TEMPORARY — CW-05 stopgap for juniper-cascor#319 end-to-end verification.
# Mounts the cascor src into the worker and points --cascor-path at it so the worker
# can import `candidate_unit` and actually execute remote candidate tasks.
# NOT a permanent fix — the canonical fix is the juniper-cascor-core package
# (juniper-cascor-worker#97, CW-05 Approach A). Remove this file after verification.
#
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.cw05-stopgap.yml \
#     --profile full up -d juniper-cascor-worker
services:
  juniper-cascor-worker:
    volumes:
      - /home/pcalnon/Development/python/Juniper/worktrees/juniper-cascor--fix--cascor-319-remote-collect-timeout--20260603-1511--fff242d3/src:/cascor-src:ro
    command:
      - "--server-url"
      - "ws://juniper-cascor:8200/ws/v1/workers"
      - "--cascor-path"
      - "/cascor-src"
